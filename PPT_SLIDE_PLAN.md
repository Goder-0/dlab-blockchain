# PPT 슬라이드 구성안

이 문서는 NotebookLM이나 다른 AI에 넣어 PPT 초안을 만들기 위한 기준 문서입니다.

## 목표

- 학생 실습
- 교사 시연
- 수료증 발급

이 3단계를 하나의 스토리로 묶는다.

## 슬라이드 1. 제목

제목 예시:
- `블록체인은 돈 기술일까, 기록 기술일까?`

핵심 메시지:
- 오늘은 코인이 아니라 기록과 검증 관점으로 본다

## 슬라이드 2. 오늘 할 일

- 학생 실습: 반장 선거 기록
- 교사 시연: 블록 생성과 테스트넷 기록
- 마지막: MetaMask 지갑과 수료증 발급

## 슬라이드 3. 아주 단순한 해시 함수

넣을 코드:

```python
def simple_hash(text):
    return len(text)
```

핵심 메시지:
- 함수는 입력을 받아 결과를 만든다
- 이 함수도 같은 입력이면 같은 결과가 나온다
- 하지만 너무 단순하다

예시:

```python
text1 = "민수:지민"
text2 = "민수:서준"
```

## 슬라이드 4. 실제 해시 함수

넣을 코드:

```python
import hashlib

def make_hash(text):
    return hashlib.sha256(text.encode("utf-8")).hexdigest()
```

핵심 메시지:
- 같은 기록은 같은 해시
- 한 글자만 바뀌어도 결과가 크게 달라진다

예시:

```python
vote1 = "민수:지민"
vote2 = "민수:서준"
```

## 슬라이드 5. 기록 연결하기

넣을 코드:

```python
hash1 = make_hash(vote1)
hash2 = make_hash(vote2 + "|" + hash1)
```

핵심 메시지:
- 두 번째 기록은 첫 번째 기록의 해시도 함께 가진다
- 앞 기록이 바뀌면 뒤 기록도 다시 계산해야 한다

## 슬라이드 6. 유효한 표만 채택하기

넣을 코드:

```python
for voter, choice in votes:
    if voter == "":
        ...
    elif choice not in choices:
        ...
    elif voter in voted_names:
        ...
    else:
        ...
```

핵심 메시지:
- 시스템은 모든 표를 다 받지 않는다
- 규칙을 통과한 표만 채택한다

예시 데이터:

```python
votes = [
    ("민수", "지민"),
    ("민수", "서준"),
    ("서연", "유나"),
    ("지우", "하린"),
]
```

## 슬라이드 7. 학생 실습에서 얻는 것

- 해시
- 연결
- 검증

교사용 메모:
- 코드가 어렵지 않아야 한다
- 한 파일을 위에서 아래로 읽을 수 있어야 한다

## 슬라이드 8. 교사 시연: live_board

보여줄 화면:
- 거래 입력
- 대기열
- 채굴 버튼
- balances
- accepted / rejected

핵심 메시지:
- 우리가 한 아이디어를 더 큰 시스템처럼 본다
- 규칙을 통과한 거래만 블록에 들어간다

## 슬라이드 9. 실제 테스트넷 기록

보여줄 화면:
- `테스트넷 기록 완료`
- `tx hash`
- `Etherscan 열기`

핵심 메시지:
- 로컬 시뮬레이션에서 끝나는 것이 아니다
- 실제 테스트넷에도 기록을 남길 수 있다

교사용 메모:
- 탐색기는 메인 설명 화면이 아니라 증빙 화면으로 쓴다

## 슬라이드 10. 지갑과 MetaMask

핵심 메시지:
- 블록체인에서 기록을 남기려면 주소와 서명이 필요하다
- 지갑은 디지털 신분증 + 서명 도구처럼 볼 수 있다

보여줄 것:
- 테스트넷 지갑 주소
- Sepolia 네트워크

## 슬라이드 11. 수료증 발급

핵심 메시지:
- 블록체인에는 돈 거래만 기록하는 것이 아니다
- 수료증, 배지, 인증서도 기록 대상으로 설계할 수 있다

추천 화면:
- MetaMask에서 학생 지갑 주소 복사
- 교사용 수료증 페이지에서 지갑 주소 입력
- `수료증 발급` 버튼 클릭
- `tx hash`와 발급 완료 메시지 확인

## 슬라이드 12. 마무리 질문

- 왜 그냥 DB가 아니라 검증 가능한 기록이 필요할까?
- 왜 잘못된 기록은 거절해야 할까?
- 블록체인은 돈 말고 어디에 쓸 수 있을까?
- 좋은 기술이 항상 바로 채택되지는 않는 이유는 뭘까?

## AI용 한 줄 프롬프트 예시

```text
첨부 문서를 바탕으로 초6~고등 대상 80분 블록체인 특강용 PPT를 12장 내외로 만들어줘. 학생 실습, 교사 시연, 수료증 발급의 3단계 흐름이 보이게 하고, 코드 예시는 너무 길지 않게 핵심 줄만 넣어줘.
```
